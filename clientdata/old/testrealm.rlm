[realm]
framecap=true
debug=false

name="Test realm!"
background="bigbackground3.png"
width=2000.
height=2000.
wallToughness=2.
speedCap=10000.
datadir="data/"
map="testmap.map"

# This should really be in server settings, buuuut...
# Well, so shouldld the framecap and debug values, really.
# It would be handy if we could optionally override certain server settings
# in the realm...
throttleInterval=200
